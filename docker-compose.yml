services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: alpha
      POSTGRES_USER: alpha
      POSTGRES_PASSWORD: alpha
    ports:
      - "5432:5432"
    volumes:
      - alpha_pg_data:/var/lib/postgresql/data

volumes:
  alpha_pg_data:
